---
phase: 01-security-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - biome.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/types/index.ts
  - packages/shared/src/query-builders/sql.ts
  - packages/shared/src/query-builders/cypher.ts
  - packages/shared/src/auth/guards.ts
  - packages/shared/src/validation/sanitize.ts
  - packages/shared/vitest.config.ts
autonomous: true
requirements:
  - SEC-12

must_haves:
  truths:
    - "Running `npm install` at repo root resolves all workspace packages"
    - "@epstein/shared package exports query builders, auth guards, validation, and types"
    - "Biome check runs across the entire repository from root"
    - "Shared package compiles with zero TypeScript errors"
  artifacts:
    - path: "package.json"
      provides: "Root workspace configuration"
      contains: "workspaces"
    - path: "packages/shared/src/index.ts"
      provides: "Barrel export for all shared modules"
      exports: ["buildFulltextSearchQuery", "buildCappedCountQuery", "buildTraversalQuery", "validateRelationshipTypes", "createRequireApiKey", "createRequireAuth", "sanitizeSearchSnippet"]
    - path: "packages/shared/src/query-builders/sql.ts"
      provides: "Parameterized SQL query builders"
      exports: ["buildFulltextSearchQuery", "buildCappedCountQuery"]
    - path: "packages/shared/src/query-builders/cypher.ts"
      provides: "Cypher query builders with allowlist validation"
      exports: ["buildTraversalQuery", "validateRelationshipTypes", "ALLOWED_RELATIONSHIP_TYPES", "ALLOWED_NODE_LABELS"]
    - path: "packages/shared/src/auth/guards.ts"
      provides: "Fail-closed auth middleware for Hono"
      exports: ["createRequireApiKey", "createRequireAuth"]
    - path: "biome.json"
      provides: "Root Biome configuration"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/query-builders/sql.ts"
      via: "barrel re-export"
      pattern: "export.*from.*query-builders/sql"
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/query-builders/cypher.ts"
      via: "barrel re-export"
      pattern: "export.*from.*query-builders/cypher"
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/auth/guards.ts"
      via: "barrel re-export"
      pattern: "export.*from.*auth/guards"
---

<objective>
Create the monorepo workspace foundation and @epstein/shared package containing parameterized query builders, fail-closed auth guards, input validation/sanitization utilities, and shared TypeScript types. This is the foundation that all subsequent Phase 1 plans import from.

Purpose: SEC-12 requires extracting shared modules to eliminate duplication. Every security fix in Plan 02 and every performance fix in Plan 03 depends on importing from this package. Building it first prevents circular dependencies and ensures consistent patterns.

Output: Root workspace config, Biome config, fully typed @epstein/shared package with query builders (SQL + Cypher), auth guards, validation utilities, and shared types.
</objective>

<execution_context>
@C:/Users/Carl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-foundation/01-CONTEXT.md
@.planning/phases/01-security-and-foundation/01-RESEARCH.md

@frontend/src/lib/types/index.ts
@frontend/src/lib/server/db.ts
@frontend/src/lib/server/neo4j.ts
@frontend/src/hooks.server.ts
@mcp-http-proxy/index.js
@cloudflare-worker/api-backend/fast-processor.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo workspace and shared package scaffold</name>
  <files>package.json, biome.json, packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/vitest.config.ts</files>
  <action>
1. Create root `package.json` with npm workspaces:
   - `"workspaces": ["packages/*", "frontend", "cloudflare-worker", "mcp-http-proxy"]`
   - Add scripts: `"test": "npm test --workspaces --if-present"`, `"check": "npx @biomejs/biome check ."`, `"format": "npx @biomejs/biome format --write ."`
   - `"private": true`

2. Create `biome.json` at repo root:
   - Formatter: indent with tabs (matches SvelteKit convention), line width 100
   - Linter: recommended rules enabled
   - Organize imports enabled
   - Ignore: `node_modules`, `.svelte-kit`, `dist`, `build`, `data`, `backups`, `logs`
   - Files: include `*.ts`, `*.js`, `*.svelte`, `*.json`

3. Create `packages/shared/package.json`:
   - Name: `@epstein/shared`
   - `"type": "module"`
   - `"main": "src/index.ts"` (consumers will use TypeScript directly via workspace resolution)
   - `"types": "src/index.ts"`
   - `"exports": { ".": "./src/index.ts" }`
   - devDependencies: `vitest`, `typescript`
   - dependencies: `hono` (for auth guard types)

4. Create `packages/shared/tsconfig.json`:
   - Target: ES2022, module: ESNext, moduleResolution: bundler
   - strict: true, declaration: true
   - Include: `src/**/*.ts`

5. Create `packages/shared/vitest.config.ts`:
   - Basic vitest config, no special plugins needed (pure TypeScript)

6. Run `npm install` at root to link workspaces.

Note: Do NOT add `isomorphic-dompurify` to the shared package — per Research pitfall #2, DOMPurify only works in DOM environments. The sanitize module exports config/types only; actual DOMPurify calls happen in the frontend.
  </action>
  <verify>
    <automated>cd C:/Users/Carl/projects/epstein-investigation-platform && node -e "const pkg = require('./package.json'); console.assert(pkg.workspaces.includes('packages/*'), 'workspaces missing'); console.log('OK')" && cd packages/shared && node -e "const pkg = require('./package.json'); console.assert(pkg.name === '@epstein/shared', 'name wrong'); console.log('OK')"</automated>
  </verify>
  <done>Root package.json has workspaces config, biome.json exists, @epstein/shared package scaffolded with tsconfig and vitest config, npm install succeeds with workspace linking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared query builders, auth guards, and types</name>
  <files>packages/shared/src/index.ts, packages/shared/src/types/index.ts, packages/shared/src/query-builders/sql.ts, packages/shared/src/query-builders/cypher.ts, packages/shared/src/auth/guards.ts, packages/shared/src/validation/sanitize.ts</files>
  <behavior>
    - buildFulltextSearchQuery("test query") returns object with parameterized SQL using plainto_tsquery, never ILIKE
    - buildFulltextSearchQuery with empty string throws or returns error
    - buildCappedCountQuery("search_vector @@ plainto_tsquery('english', $1)") returns SQL with LIMIT 10001 CTE
    - validateRelationshipTypes(["MENTIONED_IN", "EVIL_INJECTION"]) returns only ["MENTIONED_IN"]
    - validateRelationshipTypes([]) returns []
    - buildTraversalQuery(["MENTIONED_IN"], 2) returns parameterized Cypher with no string interpolation of user input
    - buildTraversalQuery with depth > 4 clamps to 4
    - buildTraversalQuery with depth < 1 clamps to 1
    - createRequireApiKey("secret") returns middleware that rejects requests without matching X-API-Key
    - createRequireApiKey("") throws Error (fail-closed — refuse to create guard with empty key)
    - createRequireAuth middleware rejects when Cf-Access-Authenticated-User-Email header missing
    - sanitizeConfig objects define allowed tags/attrs for search snippets, chat content, and document text
  </behavior>
  <action>
1. Create `packages/shared/src/types/index.ts`:
   - Copy and refine types from `frontend/src/lib/types/index.ts` (SearchResult, Document, Entity, GraphNode, ChatMessage, Citation, etc.)
   - Add query builder types: `SqlQuery { text: string; values: unknown[] }`, `CypherQuery { query: string; params: Record<string, unknown> }`
   - Add auth types: `AuthUser { email: string }`, middleware type aliases for Hono

2. Create `packages/shared/src/query-builders/sql.ts`:
   - `buildFulltextSearchQuery(query: string, options: { limit?: number; offset?: number; source?: string }): SqlQuery` — uses `search_vector @@ plainto_tsquery('english', $1)` with parameterized pagination. Never uses ILIKE.
   - `buildCappedCountQuery(whereClause: string, params: unknown[]): SqlQuery` — wraps in CTE with LIMIT 10001. Frontend shows "10,000+" when count equals 10001.
   - `buildDocumentStatsQuery(): SqlQuery` — replaces raw stat queries from MCP proxy
   - `buildEntityListQuery(options: { type?: string; limit?: number; offset?: number }): SqlQuery`

3. Create `packages/shared/src/query-builders/cypher.ts`:
   - `ALLOWED_RELATIONSHIP_TYPES: Set<string>` — populated with known types. Include comment that these should be refreshed from `CALL db.relationshipTypes()` on the live DB. Start with: MENTIONED_IN, CONNECTED_TO, ASSOCIATED_WITH, WORKS_FOR, LOCATED_IN, RELATED_TO, APPEARED_WITH, EMPLOYED_BY, OWNS, MEMBER_OF.
   - `ALLOWED_NODE_LABELS: Set<string>` — Person, Organization, Location, Document, Event
   - `validateRelationshipTypes(types: string[]): string[]` — filters against allowlist
   - `validateNodeLabels(labels: string[]): string[]` — filters against allowlist
   - `buildTraversalQuery(relationshipTypes: string[], maxDepth: number): CypherQuery` — clamps depth 1-4, validates types against allowlist, uses parameterized pattern with NO string interpolation of user input
   - `buildNeighborsQuery(): CypherQuery` — parameterized neighbor lookup
   - `buildShortestPathQuery(): CypherQuery` — parameterized shortest path

4. Create `packages/shared/src/auth/guards.ts`:
   - `createRequireApiKey(apiSecretKey: string): MiddlewareHandler` — Hono middleware. Throws if apiSecretKey is empty/falsy (fail-closed per CR-007). Checks `X-API-Key` header.
   - `createRequireAuth(): MiddlewareHandler` — Hono middleware for Cloudflare Access. Checks `Cf-Access-Authenticated-User-Email` header. Returns 401 if missing.
   - Both return JSON `{ error: 'Unauthorized' }` with 401 status on failure.

5. Create `packages/shared/src/validation/sanitize.ts`:
   - Export sanitization CONFIG objects (not DOMPurify calls — those happen in frontend):
     - `SEARCH_SNIPPET_SANITIZE_CONFIG = { ALLOWED_TAGS: ['mark'], ALLOWED_ATTR: [] }`
     - `CHAT_CONTENT_SANITIZE_CONFIG = { ALLOWED_TAGS: ['p', 'br', 'strong', 'em', 'code', 'pre', 'ul', 'ol', 'li', 'a', 'mark'], ALLOWED_ATTR: ['href', 'class'] }`
     - `DOCUMENT_TEXT_SANITIZE_CONFIG = { ALLOWED_TAGS: ['mark', 'span'], ALLOWED_ATTR: ['class'] }`
   - `validateSearchQuery(query: string): string` — trims, rejects empty, caps at 500 chars
   - `validatePaginationParams(page: unknown, limit: unknown): { page: number; limit: number }` — coerces to positive integers, caps limit at 100

6. Create `packages/shared/src/index.ts`:
   - Barrel export everything from all submodules

7. Write tests FIRST (TDD): Create test files for sql.ts, cypher.ts, guards.ts, sanitize.ts before implementing. Run tests (expect RED), then implement (expect GREEN).
  </action>
  <verify>
    <automated>cd C:/Users/Carl/projects/epstein-investigation-platform && npx vitest run --project shared 2>/dev/null || cd packages/shared && npx vitest run</automated>
  </verify>
  <done>All shared package tests pass. Query builders produce parameterized SQL/Cypher with no string interpolation. Auth guards fail closed on empty keys. Validation rejects invalid input. Types are exported and importable. `npx tsc --noEmit` in packages/shared passes with zero errors.</done>
</task>

</tasks>

<verification>
1. `npm install` at repo root succeeds and links @epstein/shared
2. `cd packages/shared && npx vitest run` — all tests pass
3. `cd packages/shared && npx tsc --noEmit` — zero TypeScript errors
4. `npx @biomejs/biome check packages/shared/src/` — no lint errors
5. Import test: `node -e "import('@epstein/shared')"` resolves from another workspace package
</verification>

<success_criteria>
- Root workspace configured with npm workspaces
- @epstein/shared package exports query builders, auth guards, validation, and types
- All shared package unit tests pass
- Zero TypeScript errors in shared package
- Biome configured and passing on shared package code
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-foundation/01-01-SUMMARY.md`
</output>
