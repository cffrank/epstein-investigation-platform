---
phase: 01-security-and-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/routes/api/search/+server.ts
  - frontend/src/routes/api/chat/+server.ts
  - frontend/src/lib/features/chat/stores.svelte.ts
  - cloudflare-worker/src/index.ts
autonomous: true
requirements:
  - SEC-07
  - SEC-08
  - SEC-09
  - SEC-10
  - SEC-11

must_haves:
  truths:
    - "Search queries use plainto_tsquery against search_vector, never ILIKE on extracted_text"
    - "Pagination count queries use capped CTE with LIMIT 10001"
    - "Hybrid search skips COUNT in fulltext leg and uses Qdrant native offset"
    - "Chat context is bounded to sliding window of last 6 messages"
    - "Worker batch processing uses Promise.all with chunks of 5 instead of sequential loop"
  artifacts:
    - path: "frontend/src/routes/api/search/+server.ts"
      provides: "Optimized search with FTS and capped counts"
      contains: "plainto_tsquery"
    - path: "frontend/src/routes/api/chat/+server.ts"
      provides: "Bounded chat context"
      contains: "MAX_CONTEXT_MESSAGES"
    - path: "cloudflare-worker/src/index.ts"
      provides: "Parallel batch processing"
      contains: "Promise.all"
  key_links:
    - from: "frontend/src/routes/api/search/+server.ts"
      to: "@epstein/shared"
      via: "import query builders"
      pattern: "import.*buildFulltextSearchQuery.*@epstein/shared"
    - from: "frontend/src/routes/api/search/+server.ts"
      to: "@epstein/shared"
      via: "import capped count"
      pattern: "import.*buildCappedCountQuery.*@epstein/shared"
---

<objective>
Fix all performance bottlenecks: replace ILIKE with full-text search (SEC-07), optimize COUNT queries (SEC-08), tune hybrid search (SEC-09), bound chat context (SEC-10), and parallelize batch processing (SEC-11).

Purpose: The platform has 961K documents. ILIKE full-table scans and unbounded COUNTs are the primary performance killers. Chat context growing unboundedly wastes tokens. Batch processing running sequentially wastes time.

Output: Sub-2-second search on 961K docs, capped pagination counts, efficient hybrid search, bounded chat context, parallel batch processing.
</objective>

<execution_context>
@C:/Users/Carl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-foundation/01-CONTEXT.md
@.planning/phases/01-security-and-foundation/01-RESEARCH.md
@.planning/phases/01-security-and-foundation/01-01-SUMMARY.md

@frontend/src/routes/api/search/+server.ts
@frontend/src/routes/api/chat/+server.ts
@frontend/src/lib/features/chat/stores.svelte.ts
@frontend/src/lib/features/chat/sse.ts
@cloudflare-worker/src/index.ts

<interfaces>
<!-- From @epstein/shared (created in Plan 01) -->
From packages/shared/src/query-builders/sql.ts:
```typescript
export interface SqlQuery { text: string; values: unknown[] }
export function buildFulltextSearchQuery(query: string, options?: { limit?: number; offset?: number; source?: string }): SqlQuery;
export function buildCappedCountQuery(whereClause: string, params: unknown[]): SqlQuery;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize search and pagination performance (SEC-07, SEC-08, SEC-09)</name>
  <files>frontend/src/routes/api/search/+server.ts</files>
  <action>
1. **SEC-07: Replace ILIKE with full-text search** in `frontend/src/routes/api/search/+server.ts`:
   - Find all instances of `ILIKE` or `ilike` in the search endpoint
   - Replace with `search_vector @@ plainto_tsquery('english', $1)` using `buildFulltextSearchQuery` from `@epstein/shared`
   - The `search_vector` tsvector column already exists on the documents table and is indexed
   - For person mention count queries (if any use ILIKE on extracted_text), prefer Neo4j entity lookup: `MATCH (p:Person {name: $name})-[:MENTIONED_IN]->(d:Document) RETURN count(d)`

2. **SEC-08: Optimize COUNT queries** in the same file:
   - Find the fulltext search COUNT(*) query used for pagination total
   - Replace with capped count CTE using `buildCappedCountQuery` from `@epstein/shared`
   - Update the frontend response to handle `count === 10001` by displaying "10,000+ results" in the UI
   - The capped count avoids scanning all 961K rows for pagination

3. **SEC-09: Optimize hybrid search**:
   - In the hybrid search path, skip the COUNT query entirely for the fulltext leg — hybrid search doesn't need exact totals for the fulltext component since results are merged via RRF
   - For the Qdrant (semantic) leg of hybrid search, use Qdrant's native `offset` parameter instead of fetching extra results and slicing in memory
   - Ensure RRF (Reciprocal Rank Fusion) merge still works correctly with these optimizations
  </action>
  <verify>
    <automated>cd C:/Users/Carl/projects/epstein-investigation-platform && grep -n "ILIKE\|ilike" frontend/src/routes/api/search/+server.ts && echo "FAIL: ILIKE still present" || echo "PASS: no ILIKE in search"</automated>
  </verify>
  <done>Search uses plainto_tsquery (not ILIKE). Pagination uses capped COUNT with 10001 limit. Hybrid search skips COUNT in fulltext leg and uses Qdrant native offset. All queries parameterized via @epstein/shared builders.</done>
</task>

<task type="auto">
  <name>Task 2: Bound chat context and parallelize batch processing (SEC-10, SEC-11)</name>
  <files>frontend/src/routes/api/chat/+server.ts, frontend/src/lib/features/chat/stores.svelte.ts, cloudflare-worker/src/index.ts</files>
  <action>
1. **SEC-10: Fix unbounded chat context** in `frontend/src/routes/api/chat/+server.ts` and/or `frontend/src/lib/features/chat/stores.svelte.ts`:
   - Find where messages array is sent to the AI API (look for the body JSON.stringify with messages)
   - Add sliding window: `const MAX_CONTEXT_MESSAGES = 6;`
   - If messages.length > MAX_CONTEXT_MESSAGES:
     - Always include the first message (system context/first user message)
     - Include the last (MAX_CONTEXT_MESSAGES - 1) messages for conversation continuity
   - If messages.length <= MAX_CONTEXT_MESSAGES, send all messages
   - Apply this in the server-side API route (not just the client store) to ensure the backend enforces the limit regardless of client behavior

2. **SEC-11: Parallelize Worker batch processing** in `cloudflare-worker/src/index.ts`:
   - Find the `/process/batch` route handler (or similar batch processing endpoint)
   - Find the sequential `for` loop that processes documents one at a time
   - Replace with chunked parallel processing:
     ```typescript
     const CHUNK_SIZE = 5;
     for (let i = 0; i < documents.length; i += CHUNK_SIZE) {
       const chunk = documents.slice(i, i + CHUNK_SIZE);
       const results = await Promise.all(
         chunk.map(doc => processDocument(doc, c.env))
       );
       allResults.push(...results);
     }
     ```
   - Ensure error handling: if one document in a chunk fails, the others still succeed (use Promise.allSettled if needed, or catch per-document)
   - Keep the total batch size limit (likely 50) but process in parallel chunks within each batch
  </action>
  <verify>
    <automated>cd C:/Users/Carl/projects/epstein-investigation-platform && grep -n "MAX_CONTEXT_MESSAGES" frontend/src/routes/api/chat/+server.ts && grep -n "Promise.all" cloudflare-worker/src/index.ts | head -5 && echo "PASS" || echo "FAIL: missing optimizations"</automated>
  </verify>
  <done>Chat context bounded to 6-message sliding window (enforced server-side). Batch processing uses Promise.all with chunks of 5 instead of sequential loop. Error handling preserves per-document isolation.</done>
</task>

</tasks>

<verification>
1. `grep -n "ILIKE\|ilike" frontend/src/routes/api/search/+server.ts` — no ILIKE
2. `grep -n "plainto_tsquery\|search_vector" frontend/src/routes/api/search/+server.ts` — present
3. `grep -n "10001\|LIMIT 10001" frontend/src/routes/api/search/+server.ts` — capped count present
4. `grep -n "MAX_CONTEXT_MESSAGES" frontend/src/routes/api/chat/+server.ts` — sliding window present
5. `grep -n "Promise.all" cloudflare-worker/src/index.ts` — parallel processing present
</verification>

<success_criteria>
- Search queries use full-text search (plainto_tsquery), never ILIKE
- COUNT queries capped at 10001 for pagination
- Hybrid search optimized (no COUNT in fulltext leg, native Qdrant offset)
- Chat context bounded to 6-message sliding window
- Batch processing parallelized with chunks of 5
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-foundation/01-03-SUMMARY.md`
</output>
