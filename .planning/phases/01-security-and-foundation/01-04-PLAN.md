---
phase: 01-security-and-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - .github/workflows/ci.yml
  - frontend/vitest.config.ts
  - frontend/src/hooks.server.test.ts
  - frontend/src/lib/features/chat/sse.test.ts
  - frontend/src/lib/utils/sanitize.test.ts
  - cloudflare-worker/vitest.config.ts
  - frontend/src/app.d.ts
  - frontend/package.json
  - cloudflare-worker/package.json
autonomous: true
requirements:
  - SEC-14
  - SEC-15
  - SEC-16

must_haves:
  truths:
    - "CI pipeline runs Vitest, svelte-check, tsc --noEmit, Biome check, build verification, and npm audit on every push"
    - "CI blocks merge when any check fails"
    - "Frontend Vitest suite has tests for auth guards, SSE parser, and sanitize utilities"
    - "Frontend compiles with zero TypeScript errors (svelte-check + tsc --noEmit)"
    - "CI auto-deploys frontend to Cloudflare Pages and backend to Hetzner on merge to main"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Full CI/CD pipeline"
      contains: "vitest"
    - path: "frontend/vitest.config.ts"
      provides: "Frontend Vitest configuration"
      contains: "svelte"
    - path: "frontend/src/hooks.server.test.ts"
      provides: "Auth guard tests"
      contains: "401"
    - path: "frontend/src/lib/features/chat/sse.test.ts"
      provides: "SSE parser tests"
      contains: "parseSSE"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "npm test"
      via: "CI test step"
      pattern: "npm test"
    - from: ".github/workflows/ci.yml"
      to: "svelte-check"
      via: "CI type check step"
      pattern: "svelte-check"
    - from: ".github/workflows/ci.yml"
      to: "biome check"
      via: "CI lint step"
      pattern: "biome check"
---

<objective>
Establish test infrastructure (Vitest for frontend + shared), write critical-path tests (~15-20 tests for auth guards, SSE parser, sanitize utils, SQL/Cypher builders), set up CI/CD pipeline extending existing GitHub Actions workflow, and resolve all 20 frontend TypeScript errors.

Purpose: SEC-15 requires test infrastructure and critical path tests. SEC-14 requires CI/CD that blocks on failures. SEC-16 requires zero TS errors. This is the quality gate that ensures all security and performance fixes from Plans 02-03 stay fixed as new features ship.

Output: Vitest configs, ~15-20 tests, CI pipeline with test/lint/type-check/build/deploy gates, zero frontend TS errors.
</objective>

<execution_context>
@C:/Users/Carl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-foundation/01-CONTEXT.md
@.planning/phases/01-security-and-foundation/01-RESEARCH.md
@.planning/phases/01-security-and-foundation/01-01-SUMMARY.md
@.planning/phases/01-security-and-foundation/01-02-SUMMARY.md
@.planning/phases/01-security-and-foundation/01-03-SUMMARY.md

@.github/workflows/deploy-frontend.yml
@frontend/src/hooks.server.ts
@frontend/src/lib/features/chat/sse.ts
@frontend/src/app.d.ts
@frontend/package.json
@frontend/tsconfig.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Set up Vitest and write critical-path tests (SEC-15)</name>
  <files>frontend/vitest.config.ts, frontend/package.json, frontend/src/hooks.server.test.ts, frontend/src/lib/features/chat/sse.test.ts, frontend/src/lib/utils/sanitize.test.ts</files>
  <behavior>
    Auth guard tests (hooks.server.test.ts):
    - Request to /api/search with Cf-Access-Authenticated-User-Email returns non-401
    - Request to /api/search without auth header returns 401
    - Request to non-/api/ path without auth header is allowed through
    - Request sets event.locals.user when auth header present

    SSE parser tests (sse.test.ts):
    - parseSSE("data: hello\n\n") yields event with data "hello"
    - parseSSE with multiple events yields all events in order
    - parseSSE with event: field correctly parses event type
    - parseSSE handles partial chunks (buffering across calls)
    - parseSSE handles empty data lines

    Sanitize utility tests (sanitize.test.ts):
    - sanitizeSearchSnippet preserves mark tags
    - sanitizeSearchSnippet strips script tags
    - sanitizeSearchSnippet strips onclick attributes
    - sanitizeChatContent preserves allowed formatting tags (p, strong, em, code, a)
    - sanitizeChatContent strips iframe and script
    - sanitizeDocumentText preserves mark and span with class
  </behavior>
  <action>
1. Install Vitest in frontend:
   - `cd frontend && npm install --save-dev vitest`
   - If SSE parser or hooks tests need mocking, no extra libraries needed (Vitest has built-in vi.mock)

2. Create `frontend/vitest.config.ts`:
   - Import from vitest/config
   - Set resolve alias for `$lib` to `./src/lib` (matches SvelteKit's path alias)
   - Set resolve alias for `$app` to mock paths if needed
   - Environment: 'node' (server-side tests, no DOM needed for hooks/sse)
   - Include: `src/**/*.test.ts`

3. Write tests FIRST (RED phase):

   `frontend/src/hooks.server.test.ts` (~5 tests):
   - Mock the handle function from hooks.server.ts
   - Test that requests to /api/* without Cf-Access-Authenticated-User-Email get 401
   - Test that requests to /api/* with the header pass through and set locals.user
   - Test that non-API paths pass through without auth check
   - Test edge cases: empty email header, malformed paths

   `frontend/src/lib/features/chat/sse.test.ts` (~5 tests):
   - Import the SSE parser function
   - Test parsing single event: `data: content\n\n`
   - Test parsing multiple events in one chunk
   - Test parsing events with event type field
   - Test buffering behavior across partial chunks
   - Test edge cases: empty data, malformed events

   `frontend/src/lib/utils/sanitize.test.ts` (~6 tests):
   - Import sanitize functions (these were created in Plan 02)
   - Test sanitizeSearchSnippet: preserves `<mark>`, strips `<script>`, strips `onerror`
   - Test sanitizeChatContent: preserves `<p><strong><em><code><a>`, strips `<iframe><script>`
   - Test sanitizeDocumentText: preserves `<mark><span class="...">`, strips everything else

4. Run tests — expect RED (functions may need import path adjustments).

5. Fix any import issues to get tests GREEN. The actual implementations already exist from Plans 01-02. Tests should pass against existing code.

6. Add test script to frontend package.json: `"test": "vitest run"`
  </action>
  <verify>
    <automated>cd C:/Users/Carl/projects/epstein-investigation-platform/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Frontend Vitest configured with $lib alias. ~15-20 tests written and passing for auth guards, SSE parser, and sanitize utilities. All tests use mocked dependencies (no live DB). `npm test` in frontend passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create CI/CD pipeline and fix TypeScript errors (SEC-14, SEC-16)</name>
  <files>.github/workflows/ci.yml, .github/workflows/deploy-frontend.yml, frontend/src/app.d.ts</files>
  <action>
1. **SEC-16: Fix all frontend TypeScript errors**:
   - Run `cd frontend && npx svelte-check --tsconfig ./tsconfig.json` to get the full error list
   - Fix all 20 errors. Common patterns:
     - Missing type annotations on function parameters
     - Type mismatches between API response shapes and component props
     - Missing null checks on optional properties
     - `app.d.ts` declarations that don't match actual usage
   - Run `npx svelte-check` again — must be zero errors
   - Also run `npx tsc --noEmit` to catch any non-Svelte TS errors

2. **SEC-14: Create CI/CD pipeline** (`.github/workflows/ci.yml`):
   - Rename or supersede existing `deploy-frontend.yml` (keep the old file but have ci.yml be the primary)
   - Trigger: push to main, pull_request to main (remove path filters — CI should run on all changes, not just frontend)
   - Jobs:

   **Job 1: test-and-lint** (runs on all pushes and PRs):
   ```yaml
   steps:
     - checkout
     - setup-node 22, cache npm
     - npm ci (at root — installs all workspaces)
     - Vitest: npm test --workspaces --if-present
     - TypeScript (frontend): cd frontend && npx svelte-check --tsconfig ./tsconfig.json
     - TypeScript (worker): cd cloudflare-worker && npx tsc --noEmit
     - Biome: npx @biomejs/biome check .
     - Build frontend: cd frontend && npm run build
     - npm audit: npm audit --audit-level=high (allow moderate, fail on high+)
   ```

   **Job 2: deploy-frontend** (only on push to main, needs test-and-lint):
   ```yaml
   steps:
     - checkout, setup-node, npm ci
     - Build: cd frontend && npm run build
     - Deploy: cloudflare/wrangler-action pages deploy
   ```

   **Job 3: deploy-backend** (only on push to main, needs test-and-lint):
   ```yaml
   steps:
     - checkout
     - SSH deploy: ssh to Hetzner, git pull, docker compose up -d --build
     - Use SSH key from GitHub secrets (SSH_PRIVATE_KEY)
     - Commands: ssh root@88.99.61.233 'cd /opt/app && git pull && docker compose up -d --build mcp-http-proxy epstein-api-backend epstein-api-backend-2 epstein-api-backend-3 epstein-api-backend-4 && docker compose restart nginx'
   ```

   - Required GitHub secrets: CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, SSH_PRIVATE_KEY, SSH_KNOWN_HOSTS
   - Branch protection: note in comments that repo settings should require "test-and-lint" to pass before merge

3. Install Biome at root for CI:
   - Add `@biomejs/biome` to root package.json devDependencies if not already present
   - Ensure `npx @biomejs/biome check .` passes on the codebase (may need to fix formatting issues — run `npx @biomejs/biome format --write .` first, then `npx @biomejs/biome check --write .` for auto-fixable lint issues)

4. Update root package.json scripts if needed:
   - `"test": "npm test --workspaces --if-present"`
   - `"lint": "npx @biomejs/biome check ."`
   - `"format": "npx @biomejs/biome format --write ."`
  </action>
  <verify>
    <automated>cd C:/Users/Carl/projects/epstein-investigation-platform && cd frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5 && cd .. && test -f .github/workflows/ci.yml && echo "PASS: ci.yml exists" || echo "FAIL: ci.yml missing"</automated>
  </verify>
  <done>All 20 frontend TypeScript errors resolved (svelte-check + tsc --noEmit pass with zero errors). CI/CD pipeline at .github/workflows/ci.yml runs: Vitest, svelte-check, tsc --noEmit, Biome check, build verification, npm audit. Deploys frontend to Cloudflare Pages and backend to Hetzner on merge to main. Merges blocked on test-and-lint failure.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx svelte-check --tsconfig ./tsconfig.json` — zero errors
2. `cd frontend && npx vitest run` — all tests pass
3. `cd packages/shared && npx vitest run` — all tests pass (from Plan 01)
4. `npx @biomejs/biome check .` — no errors
5. `.github/workflows/ci.yml` exists with test, lint, type-check, build, and deploy jobs
6. CI workflow validates: test-and-lint runs on PRs, deploy jobs only on push to main
</verification>

<success_criteria>
- Zero frontend TypeScript errors (svelte-check + tsc --noEmit)
- ~15-20 critical-path tests passing in frontend Vitest
- CI pipeline blocks merges on any test/lint/type-check/build failure
- CI auto-deploys frontend (Cloudflare Pages) and backend (Hetzner SSH) on merge to main
- Biome lint/format passing across entire codebase
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-foundation/01-04-SUMMARY.md`
</output>
