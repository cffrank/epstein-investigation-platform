---
phase: 02-ai-chat-with-tool-use
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - frontend/src/lib/features/chat/sse.test.ts
autonomous: false
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CHAT-08, CHAT-09, CHAT-10, CHAT-11]
user_setup:
  - service: cloudflare-pages
    why: "Environment variables for Anthropic API"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "ProtonPass vault AxiomAI → anthropic-master → password. Must be set in Cloudflare Pages environment variables."
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "ProtonPass vault AxiomAI → cloudflare-master → username. Must be set in Cloudflare Pages environment variables."

must_haves:
  truths:
    - "SSE parser tests pass for all new event types"
    - "User can start a chat and receive streaming responses from Claude"
    - "User can select Claude model and see it take effect"
    - "Claude calls tools during conversation and user sees tool indicators"
    - "Citations appear as clickable badges in assistant messages"
    - "AI disclaimer appears below each assistant message"
  artifacts:
    - path: "frontend/src/lib/features/chat/sse.test.ts"
      provides: "Updated tests for SSE parser with new event types"
  key_links:
    - from: "frontend/src/lib/features/chat/sse.test.ts"
      to: "$lib/features/chat/sse"
      via: "import parseSSE"
      pattern: "import.*parseSSE"
---

<objective>
Update SSE parser tests and verify the complete AI chat flow end-to-end.

Purpose: Ensure the SSE parser handles all new event types (tool_call, tool_result, text_delta, citations_delta, error, done) and verify the full chat experience works — from model selection through tool use to citation display.

Output: Updated tests passing, human verification of the live chat UI.
</objective>

<execution_context>
@C:/Users/Carl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Carl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-ai-chat-with-tool-use/02-02-SUMMARY.md
@.planning/phases/02-ai-chat-with-tool-use/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SSE parser tests for new event types</name>
  <files>frontend/src/lib/features/chat/sse.test.ts</files>
  <action>
Update `frontend/src/lib/features/chat/sse.test.ts` to cover the new SSE event types. Keep existing tests, add new ones:

1. Test `tool_call` event parsing:
```typescript
it('parses tool_call events', async () => {
  const stream = createMockStream('event: tool_call\ndata: {"id":"toolu_123","name":"search_documents"}\n\n');
  const events = [];
  for await (const event of parseSSE(stream)) events.push(event);
  expect(events).toHaveLength(1);
  expect(events[0].event).toBe('tool_call');
  expect(JSON.parse(events[0].data)).toEqual({ id: 'toolu_123', name: 'search_documents' });
});
```

2. Test `tool_result` event parsing.
3. Test `text_delta` event parsing.
4. Test `citations_delta` event with a citation object.
5. Test `error` event parsing.
6. Test `done` event parsing.
7. Test mixed event stream: `tool_call` → `tool_result` → `text_delta` → `citations_delta` → `done` in sequence.
8. Test multiple `text_delta` events accumulate correctly.

Use the existing test helper pattern for creating mock ReadableStreams from string data.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- --run src/lib/features/chat/sse.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All SSE parser tests pass including new event types. Mixed event stream test verifies correct ordering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete AI Chat with Tool Use — Claude-powered chat replacing OpenAI, with:
- Anthropic SDK routed through Cloudflare AI Gateway
- 5 investigation tools (search_documents, semantic_search, get_entity_profile, graph_query, find_connections)
- Streaming SSE with tool call indicators
- Native Anthropic citations as clickable badges
- Model selector (Haiku/Sonnet/Opus)
- AI disclaimer on each message
  </what-built>
  <how-to-verify>
1. Run `cd frontend && pnpm dev` to start the dev server
2. Open the chat page in browser

**Model Selector (CHAT-02):**
3. Verify the model dropdown appears in the chat header showing "Sonnet 4.6"
4. Change to "Opus 4.6", refresh page — verify it persists
5. Change back to "Sonnet 4.6" for cost savings

**Basic Chat (CHAT-01, CHAT-08):**
6. Type "What documents mention Jeffrey Epstein?" and send
7. Verify streaming text appears token-by-token (not in big chunks)
8. Verify response completes without errors

**Tool Use (CHAT-03 through CHAT-07):**
9. During the response, verify tool call indicators appear (e.g., "Searching documents...")
10. Verify indicators update to "complete" state with result counts
11. Try: "What connections exist between Ghislaine Maxwell and Les Wexner?" — this should trigger graph tools
12. Try: "Find documents semantically similar to 'private island'" — this should trigger semantic_search

**Citations (CHAT-09):**
13. Verify superscript numbered citation badges appear in the response text
14. Hover on a badge — verify tooltip shows document name
15. Click a badge — verify it navigates to `/documents/{id}`
16. Check the citation panel at the bottom shows cited text and source links

**Context Bounding (CHAT-10):**
17. Send 8+ messages in a conversation — verify responses remain coherent (sliding window working, not losing context catastrophically)

**AI Disclaimer (CHAT-11):**
18. Verify each assistant message has a muted italic disclaimer below it
19. Verify messages with citations say "verify claims against source documents"
20. Verify messages without citations say "may contain errors · no sources cited"

**Edge Cases:**
21. Try an empty message — verify it's rejected
22. Try while streaming — verify input is disabled
23. Click "New conversation" — verify chat clears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
- All SSE parser tests pass: `pnpm test -- --run src/lib/features/chat/sse.test.ts`
- Full TypeScript check passes: `npx tsc --noEmit`
- Human verified: chat works end-to-end with tool use and citations
</verification>

<success_criteria>
- SSE parser handles all new event types correctly
- Human confirms complete AI chat flow works as designed
- All 11 CHAT requirements verified through the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-chat-with-tool-use/02-04-SUMMARY.md`
</output>
